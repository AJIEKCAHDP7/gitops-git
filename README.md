# GitOps - Git
All about git, documentation, example, commands and etc....

🔹 Что такое Git?

Git — это распределённая система контроля версий (VCS).
Позволяет:

хранить историю изменений

работать в команде

делать ветки (branches)

откатываться к прошлым версиям

🔹 Базовый workflow
1️⃣ Инициализация репозитория
git init


Клонирование:

git clone https://github.com/user/repo.git

2️⃣ Проверка состояния
git status

3️⃣ Добавление файлов
git add file.txt
git add .

4️⃣ Коммит
git commit -m "Add initial configuration"

5️⃣ Отправка в удалённый репозиторий
git push origin main

6️⃣ Получение изменений
git pull

🔹 Работа с ветками

Создать ветку:

git branch feature-login


Переключиться:

git checkout feature-login


Создать и переключиться сразу:

git checkout -b feature-login


Слияние:

git merge feature-login

🔹 Полезные команды для sysadmin / DevOps

История:

git log --oneline


Посмотреть изменения:

git diff


Отменить изменения в файле:

git checkout -- file.txt


Откатить последний коммит (оставить изменения):

git reset --soft HEAD~1



---

🔹 3 основных состояния файла в Git

Git управляет файлами через три зоны:

Working Directory  →  Staging Area  →  Repository

1️⃣ Working Directory (рабочая директория)

📂 Это твои обычные файлы в проекте.

Состояние файла может быть:

Untracked — новый файл, Git его не отслеживает

Modified — файл изменён, но не добавлен в staging

Проверить:

git status

2️⃣ Staging Area (Index)

📦 Это промежуточная зона перед коммитом.

Когда ты делаешь:

git add file.txt


Файл становится:

Staged — готов к коммиту

3️⃣ Repository (.git)

🗂 После:

git commit -m "message"


Файл становится:

Committed — сохранён в истории Git

🔹 Полный цикл состояния файла
🔹 Новый файл
Untracked


После:

git add file.txt

Staged


После:

git commit

Tracked (clean)

🔹 Изменённый файл
Modified


После git add:

Staged


После git commit:

Unmodified

🔹 Как выглядят состояния в git status
Untracked files:
  new.txt

Changes not staged for commit:
  modified: file.txt

Changes to be committed:
  modified: config.yaml

🔹 Важные команды для управления состоянием
Убрать из staging:
git restore --staged file.txt

Отменить изменения в файле:
git restore file.txt

Удалить файл из отслеживания:
git rm file.txt
